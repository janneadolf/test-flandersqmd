renv::activate(project = file.path("..", "..", ".."))
renv::restore(
  library = file.path("..", "..", "..", "renv", "library"),
  lockfile = file.path("..", "..", "..", "renv.lock"),
  prompt = FALSE
)

